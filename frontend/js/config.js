// Конфігурація фронтенду
const CONFIG = {
    // Зміни це на свій IP/домен при виході в WAN
    // Важливо: з телефона `localhost` буде посилатися на сам телефон.
    // Тому беремо origin тієї сторінки, з якої відкрито адмін/логін.
    API_BASE_URL: `${window.location.origin}/api`
};